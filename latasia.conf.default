# listen port
port = 0

# cache servers
servers = 127.0.0.1 # localhost only by default

# pid file
pid_file = latasia.pid

# log file
log_file = latasia.log

# workers
workers = 1

# keepalive
keepalive = 60

# http_cwd
http_cwd = /
